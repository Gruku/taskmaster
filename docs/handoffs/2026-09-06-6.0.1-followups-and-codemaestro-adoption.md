# 2026-09-06 — 6.0.1 follow-up fixes and the live CodeMaestro adoption

<!-- User intent: cold-resume record for the session that shipped 6.0.1 and migrated the live CodeMaestro backlog; lists what is still unfiled. -->

## Shipped

| Repo | Commit | Pushed |
|---|---|---|
| taskmaster master | 7780d27 (6.0.1) | yes |
| claude-tools master | 6d1f2cf (submodule 6.0.1, marketplace, codex dist) | yes |
| CodeMaestro master | cf4781c (adopted backlog, scalars restored, CRLF) | no (user's repo) |

6.0.1 fixes, each on its own branch, reviewed, full-suite green on the branch, merged `--no-ff`:
- `fix/quarantine-log-lineendings` — quarantine logged once (Codex adversarial pass found 3 Important defects, all fixed in a second round); new and archive-moved files get the project's dominant line ending.
- `fix/migrate-links-depends-on` — migration keeps `depends_on`, `fixed_in_task`, `duplicate_of`; `--restore-scalars` rebuilds them from typed links (store path and storeless path).
- `fix/linear-double-push` — retry cannot requeue a live claim; enqueue failures logged and counted; queue quarantine on the Corrupt line.

Merged master: `-m "not slow"` 2263 passed, 1 skipped. The 5 stress tests were not run on the final merge (they ran green on each branch).

## CodeMaestro

Adoption ran at 12:15 local with UNFIXED code: a links sub-agent, told it "may read" the live backlog, opened it through the server. Repaired afterwards with the merged code: `scripts.migrate_links --restore-scalars` (273 tasks, 7 issues, second run no changes), `unix2dos` on the 347 LF files git listed as untracked, then a warm read (dirty 0, quarantined 4, log flat). Committed as cf4781c. Two files (`tasks/ac-slider-gate-005/006.md`) are LF on disk from before adoption; harmless.

## Still unfiled (tm MCP server was down all session)

- `depends_on` and `links` are two independent write surfaces and drift after either is used.
- Linear: 900 s lease window can still double-push; `done` rows never pruned; `failed_enqueues` never resets.
- Bootstrap `_import_row` raises ValueError on a non-mapping `project.yaml` (scan path guards it).
- `_projection_identity` stats every file per read on network roots; three dead writers in `taskmaster_v3.py`; `backlog_query` shares the write connection.
- Worktrees to remove with `guard-hooks:safe-worktree-removal` only: `.worktrees/sqlite-store-step-*` (12), `.worktrees/fix-*` (3), `.worktrees/team-relayout`, `../taskmaster-wt-0{07,19,20,24,26}`, `../tm-human-gate`. The SDD ledger and review reports sit untracked in `.worktrees/sqlite-store-step-3`.

## Session crashes

Two Claude Code crashes, both while the full suite ran detached (PowerShell `Start-Process`) during the 8-process stress test. Not memory; no test sends console interrupts. Do not run the suite detached; run `-m "not slow"` in the foreground or hand the full run to a sub-agent.
