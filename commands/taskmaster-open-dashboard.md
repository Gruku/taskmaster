---
description: Open the Taskmaster kanban dashboard in the browser
---

Call the `backlog_open_viewer` MCP tool to launch the kanban board for the current project's backlog in the user's default browser. Report the URL it returned.
