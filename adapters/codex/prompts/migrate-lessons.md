Read `{{TASKMASTER_HOME}}/playbooks/migrate-lessons/playbook.md` in full and execute it
exactly. `references/` paths inside the playbook resolve relative to the
playbook's own directory. Taskmaster's `backlog_*` MCP tools must be available
(see the taskmaster codex adapter README if they are not).

ARGUMENTS: $ARGUMENTS
