---
name: spec-review
description: "Adversarial pre-implementation design review of a task's spec. Use when the user says 'review this spec', 'challenge this design', 'is this the right approach?', 'spec review', or 'design review'. Records the spec-review (full lane) or design-review (standard lane) gate; challenges assumptions, scope, edge cases, blast radius. Does not review code — use taskmaster:review-gate for that."
---

# Spec Review

Follow the playbook at `../../playbooks/spec-review/playbook.md` (relative to
this skill's base directory). Read it in full and execute it exactly.
`references/` paths inside the playbook resolve relative to the playbook's
own directory.
