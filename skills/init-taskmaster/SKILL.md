---
name: init-taskmaster
description: "Set up Taskmaster in the current project. Triggers: 'set up taskmaster', 'initialize backlog', 'I want to track my work here', or backlog.yaml missing. Offers clean init or project analysis."
---

# Initialize Taskmaster

Follow the playbook at `../../playbooks/init-taskmaster/playbook.md` (relative to
this skill's base directory). Read it in full and execute it exactly.
`references/` paths inside the playbook resolve relative to the playbook's
own directory.
