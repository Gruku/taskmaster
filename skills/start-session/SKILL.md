---
name: start-session
description: "Start a work session, orient a new conversation. Triggers: 'let's get started', 'what should I work on', 'show me the backlog', 'orient me', or a new conversation where backlog.yaml exists. Shows dashboard, last session, next tasks."
---

# Start Session

Follow the playbook at `../../playbooks/start-session/playbook.md` (relative
to this skill's base directory). Read it in full and execute it exactly.
`references/` paths inside the playbook resolve relative to the playbook's
own directory.
